@echo off

rem  Set a variable which will remain in the DOS environment after the batch
rem  file is completed.  To verify this, enter "set" at the DOS prompt after
rem  you run this batch file.

set myvar=abc
